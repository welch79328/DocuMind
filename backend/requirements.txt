# FastAPI Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Database
sqlalchemy==2.0.35
alembic==1.14.0
psycopg2-binary==2.9.10
asyncpg==0.30.0

# Pydantic
pydantic==2.9.2
pydantic-settings==2.6.1

# AI Services
openai==1.54.5
anthropic==0.39.0

# AWS Services
boto3==1.35.60
botocore==1.35.60

# OCR (Optional)
pytesseract==0.3.13
Pillow==11.0.0
pdf2image==1.17.0
PyMuPDF==1.24.13  # PDF 處理（import 為 fitz）

# PaddleOCR (Better Chinese OCR)
# 2026-08-24 升級至 PP-OCRv6:繁中辨識平均信心度 0.63 → 0.936(同一份謄本實測)。
# paddleocr 3.7.0 預設模型即 PP-OCRv6_medium;2.8.1 的繁中走的是三代前的
# chinese_cht_PP-OCRv3_rec,與同套件簡中拿到的 v4 不同代。
# ⚠️ 需搭配 EngineManager 的 enable_mkldnn=False,理由見該處註解。
paddlepaddle==3.3.1
paddleocr==3.7.0
numpy<2.0.0
opencv-python-headless>=4.5.0  # headless 版本避免 GUI 依賴問題

# Utilities
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Development
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
